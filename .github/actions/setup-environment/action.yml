name: "Setup Environment"
description: Setup the environment for compiling the solution
runs:
  using: composite
  steps:
    - name: Set up Go and Wails
      uses: dAppServer/wails-build-action@v2.2
      with:
        go-version: "1.24"

